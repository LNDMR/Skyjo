
fun main(){

    // by calling the constructor of class 'Game' we are creating:
    // a shuffled card-deck, a discard pile
    // a human player
    // a cpu
    // each a boolean to check if all cards are flipped
    // each a boolean to check if someone actively wants to end the current round
    // a round count
    // a 'next' variable that helps to organize who's turn it is next
    // at last there is a welcome to the human player

    var skyjo: Game = Game()

    skyjo.startGame()

    // round count is set to 1
    // cpu and human player are initially choosing two card each to flip in their playing fields
    // skyjo.endGame()


}















